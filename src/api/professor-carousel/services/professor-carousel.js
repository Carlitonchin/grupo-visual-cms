'use strict';

/**
 * professor-carousel service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::professor-carousel.professor-carousel');
