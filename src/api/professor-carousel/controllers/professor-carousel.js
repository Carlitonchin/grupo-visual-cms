'use strict';

/**
 * professor-carousel controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::professor-carousel.professor-carousel');
